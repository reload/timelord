app
vendor
node_modules
.sass-cache
composer.lock
config.inc
npm-debug.log
.env

# Exclude all fonts that's not placed inside a folder (vendor fonts).
fonts/*
!fonts/*/
.DS_Store

docker/timelord.env
docker/harvester.env
docker/harvester.sqlite